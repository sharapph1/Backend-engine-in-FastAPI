{
  "name": "webx-worker-api",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "@noble/hashes": "^2.2.0",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
